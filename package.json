{
  "name": "bigbricey-ledger",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npx vercel dev",
    "start": "npx vercel dev",
    "test": "node scripts/test-coach-context.mjs && node --test test/*.test.mjs",
    "vercel-build": "echo 'static+api no build'"
  },
  "dependencies": {}
}
